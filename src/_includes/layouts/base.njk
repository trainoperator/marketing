<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{ title or site.meta.title }}</title>
  <meta name="description" content="{{ description or site.meta.description }}">
  <meta name="theme-color" content="{{ site.colors.accent }}">
  <link rel="icon" href="/assets/logo.svg" type="image/svg+xml">
  <link rel="mask-icon" href="/assets/logo.svg" color="{{ site.colors.accent }}">
  <link rel="preload" href="/assets/styles/main.css" as="style">
  <link rel="stylesheet" href="/assets/styles/main.css">
  <style>
    /* Align base layout visuals with splash layout */
    html, body { height:100%; overflow:auto; }
    body { background: var(--bg); }

    #stage {
      position: relative;
      min-height: 100svh;
      width: 100%;
      display: grid;
      place-items: center;
      padding: clamp(16px, 3vw, 28px);
      isolation: isolate;
    }
    #stage::before, #stage::after {
      content:"";
      position: absolute; inset: 0;
      z-index: -2;
      background:
        radial-gradient(120rem 120rem at 20% 10%, color-mix(in srgb, var(--accent), transparent 70%), transparent 65%),
        radial-gradient(100rem 100rem at 85% 15%, color-mix(in srgb, var(--secondary), transparent 70%), transparent 68%),
        radial-gradient(90rem 90rem at 50% 100%, color-mix(in srgb, var(--accent), white 20%), transparent 72%);
      filter: saturate(120%);
      opacity: .85;
    }
    #stage::after { z-index: -1; backdrop-filter: saturate(160%) blur(8px); }

    .panel {
      width: min(100%, 1120px);
      height: auto;
      display: grid;
      grid-template-columns: 1fr;
      grid-auto-rows: auto;
      grid-template-areas:
        "aside"
        "hero";
      gap: clamp(16px, 2.6vw, 28px);
      padding: clamp(16px, 3vw, 28px);
      border-radius: 24px;
      background: color-mix(in srgb, var(--surface), white 6%);
      border: 1px solid var(--border);
      box-shadow: var(--shadow);
      overflow: visible;
    }
    @media (min-width: 880px) and (max-height: 1080px) {
      .panel {
        max-height: calc(90svh - clamp(24px, 6vw, 48px) * 2);
        min-height: 420px;
        height: auto;
        overflow: auto;
      }
    }
    @media (min-width: 880px) {
      .panel {
        max-height: calc(55svh - clamp(24px, 6vw, 48px) * 2);
        min-height: 420px;
        height: auto;
        overflow: auto;
        grid-template-columns: minmax(0, 1.1fr) minmax(0, .9fr);
        grid-template-areas: "hero aside";
        align-items: stretch;
      }
      .hero, .aside { min-width: 0; }
      .aside { justify-self: stretch; width: min(100%, 520px); aspect-ratio: 4 / 3; display: grid; place-items: start center; padding: clamp(8px, 1.2vw, 16px); }
      #logo { width: 100%; height: auto; max-width: 100%; max-height: 100%; object-fit: contain; }
    }

    .hero { display:grid; gap:.6rem; grid-template-rows: 1fr auto; align-content:stretch; grid-area: hero; }
    .aside { grid-area: aside; display: grid; place-items: start center; }
  </style>
</head>
<body>
  <!--<header class="site-header">
    <div class="container header-inner">
      <a href="/" class="brand">
        <img src="/assets/logo.svg" alt="{{ site.name }} logo" width="300em" decoding="async" />
      </a>
      <nav class="nav">
        <a href="/">Home</a>
        <a href="/about/">About</a>
      </nav>
    </div>
  </header>
-->
  <main id="stage" role="main">
    <section class="panel">
      <div class="hero">
        <div id="content" class="container hero-inner">
          {{ content | safe }}
        </div>
      </div>
      {% if aside %}
      <div class="aside">
        {{ aside | safe }}
      </div>
      {% else %}
      <div class="aside" aria-hidden="true">
        <img id="logo" src="/assets/logo.svg" alt="{{ site.name }} logo" decoding="async" width="100%" />
      </div>
      {% endif %}
    </section>
  </main>
  <!--
  <footer class="site-footer">
    <div class="container footer-inner">
      <p>© <span id="year"></span> {{ site.name }}. All rights reserved.</p>
      <div class="social">
        {% if site.social.x %}<a href="{{ site.social.x }}" aria-label="X / Twitter">X</a>{% endif %}
        {% if site.social.github %}<a href="{{ site.social.github }}" aria-label="GitHub">GitHub</a>{% endif %}
        {% if site.social.linkedin %}<a href="{{ site.social.linkedin }}" aria-label="LinkedIn">LinkedIn</a>{% endif %}
      </div>
    </div>
  </footer>

  <script>
    const y = document.getElementById('year');
    if (y) y.textContent = new Date().getFullYear();
  </script>-->
</body>
</html>
